# CitySpace 项目说明

本项目是一组用于城市街道空间分析的 Python 脚本，主要服务于道路网络整理、空间数据转换、街景复杂度计算、平均车速分析、标签构建、训练集划分和预测结果后处理等研究流程。

## 1. 核心环境

- 编程语言：Python
- 推荐版本：Python 3.9 及以上
- 主要数据格式：CSV、Excel、Shapefile、GeoPackage
- 主要空间分析库：GeoPandas、Shapely、NetworkX、OSMnx、Cityseer

安装依赖：

```bash
python -m pip install -r requirements.txt
```

如果 `geopandas`、`osmnx`、`cityseer` 等地理空间依赖安装失败，建议使用 Conda：

```bash
conda create -n cityspace python=3.10
conda activate cityspace
conda install -c conda-forge geopandas shapely scipy scikit-learn osmnx cityseer
python -m pip install -r requirements.txt
```

## 2. 目录结构

```text
average_speed_calculation_scripts/      平均车速计算脚本
data_analysis_scripts/                  其他数据分析脚本
data_conversion_cleaning_scripts/       数据转换和清洗脚本
data_preparation_scripts/               数据准备脚本
label_train_split_scripts/              标签生成和训练集划分脚本
prediction_postprocessing_scripts/      预测结果后处理脚本
streetscape_complexity_scripts/         街景复杂度计算脚本
```

## 3. 功能模块

### 数据转换和清洗

目录：`data_conversion_cleaning_scripts/`

- `csvaddresscut.py`：CSV 地址或字段裁剪处理。
- `exceltoshp.py`：Excel 数据转空间数据。
- `to_csv.py`：数据转 CSV。
- `toshp.py`：数据转 Shapefile。

### 数据准备

目录：`data_preparation_scripts/`

- `prepare.py`、`prepare2.py`：道路网络、空间要素和分析数据准备。
- `prepareroad.py`：道路数据处理。
- `prepare语义.py`：语义或街景相关数据准备。

### 街景复杂度计算

目录：`streetscape_complexity_scripts/`

- `hill.py`：基于 Hill numbers 的多样性指标计算。
- `街景计算综合值.py`：街景复杂度综合指标计算。

### 平均车速计算

目录：`average_speed_calculation_scripts/`

- `ave_speed.py`：根据交通或道路数据计算平均速度。

### 标签与训练集划分

目录：`label_train_split_scripts/`

- `spacelink.py`：空间要素关联和标签连接。
- `tyc.py`：标签构建、区域裁剪或训练数据划分相关处理。

### 预测结果后处理

目录：`prediction_postprocessing_scripts/`

- `后处理.py`：模型预测结果的整理、优化或导出。
- `cj1_optimization.log`：优化过程日志文件。

### 其他数据分析

目录：`data_analysis_scripts/`

- `prepare3.py`、`prepare4.py`：补充数据准备或分析脚本。
- `timeanalysis.py`：时间相关分析。

## 4. 推荐使用流程

1. 根据原始数据格式，先使用 `data_conversion_cleaning_scripts/` 完成 CSV、Excel、Shapefile 等格式转换和字段清洗。
2. 使用 `data_preparation_scripts/` 准备道路网络、POI、语义信息、街景信息等分析数据。
3. 使用 `average_speed_calculation_scripts/ave_speed.py` 计算道路或街段平均车速。
4. 使用 `streetscape_complexity_scripts/` 计算街景复杂度、多样性和综合指标。
5. 使用 `label_train_split_scripts/` 完成标签关联和训练集划分。
6. 在外部训练模型或运行预测后，使用 `prediction_postprocessing_scripts/` 对结果进行后处理。
7. 结合 `data_analysis_scripts/` 中的脚本进行补充分析。

## 5. 使用注意事项

- 该项目目前是研究脚本集合，不是统一封装的命令行工具。运行前请打开具体脚本检查输入、输出路径。
- 部分脚本中存在原实验环境下的绝对路径，例如 `D:/2024srtp/...`。在自己的机器上运行前，需要改成实际数据路径。
- Shapefile 字段名有长度和字符限制，涉及中文字段时建议先清洗字段名。
- 大体积原始数据、模型输出和中间结果不建议直接提交到 Git 仓库。
- 可以使用 Trae、Codex 等 AI 编程工具辅助理解项目结构、修改路径和补充数据处理脚本。

## 6. 主要依赖

详见 `requirements.txt`。核心依赖包括：

- `pandas`、`numpy`：表格数据处理和数值计算。
- `geopandas`、`shapely`：地理空间数据读取、写入和几何处理。
- `networkx`、`cityseer`、`osmnx`：道路网络和城市空间分析。
- `scipy`、`scikit-learn`：空间索引、邻近搜索和机器学习辅助工具。
- `pypinyin`、`Unidecode`、`chardet`、`tqdm`：字段清洗、编码识别和进度显示。
